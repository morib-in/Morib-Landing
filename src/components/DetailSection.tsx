import TextBlock from '@/components/TextBlock';
import MediaBlock from '@/components/MediaBlock';

const DetailSection = () => {
  return (
    <div className="flex gap-[2rem] pl-[24rem]">
      <TextBlock />
      <MediaBlock />
    </div>
  );
};

export default DetailSection;
